import * as getters from './getters'
import * as actions from './actions'
import * as mutations from './mutations'

const state = {
     templates: [],
     pagination: {}
}

export default {
     state,
     getters,
     actions,
     mutations,
}